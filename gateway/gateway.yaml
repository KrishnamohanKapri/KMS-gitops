apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: kms-gateway
  namespace: kms
spec:
  gatewayClassName: envoy
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: Same